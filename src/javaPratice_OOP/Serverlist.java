package javaPratice_OOP;

public enum Serverlist {
	//Enum class is created when you want to define a Environment, Browser, Operation, Platform, Server. 
	//Why you use enum class, because you don't want to change the values of the variables.
	DEV, TEST, PREPROD, PRODUCTION;
}
